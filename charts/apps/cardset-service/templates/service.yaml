apiVersion: v1
kind: Service
metadata:
  name: {{ include "cardset-service.fullname" . }}
  labels: 
    {{- include "cardset-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector: 
    {{- include "cardset-service.selectorLabels" . | nindent 4 }}
